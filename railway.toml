[project]
name = "habits"

[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60

# Volume for storing exercise images
[[volumes]]
name = "exercise-images"
mount = "/data/images"
